import 'package:flutter/material.dart';

// --- Base Colors ---
const kColorBackground = Color(0xFF121212); // Dark Theme Background
const kColorSurface = Color(0xFF1E1E1E); // Generic Surface

// --- Timer Types ---
const kColorWork = Colors.redAccent;
const kColorShortBreak = Colors.greenAccent;
const kColorLongBreak = Colors.blueAccent;

// --- UI Controls ---
const kColorPrimaryAction = Colors.orangeAccent; // Start/Pause buttons
const kColorSecondaryAction = Colors.white70; // Reset/Settings buttons
const kColorSaveButton = Colors.deepPurple;

// --- Circular Timer ---
const kColorCircleInner = Color(0xFF16213E);
const kColorCircleInnerGradient = Color(0xFF0F1624);
const kColorCircleShadow = Colors.black;

// --- Flip Timer ---
const kColorFlipCard = Color(0xFF2C3E50);
const kColorFlipCardGradient = Color(0xFF34495E);
const kColorFlipShadow = Colors.black;

// --- Dialogs ---
const kColorDialogBackground = Color(0xFF16213E);

// --- Text ---
const kColorTextPrimary = Colors.white;
const kColorTextSecondary = Colors.white70;
const kColorTextMuted = Colors.white24;
